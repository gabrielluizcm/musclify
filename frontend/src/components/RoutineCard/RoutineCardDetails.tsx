export default function RoutineCardDetails({ children }: { children: React.ReactNode }) {
  return (
    <div className="flex grow-[2] flex-col text-sm items-center justify-center">
      {children}
    </div>
  )
}